# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
env/
ENV/
env.bak/
venv.bak/
.venv/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
*.log
logs/

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.nox/
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# pipenv
Pipfile.lock

# PEP 582
__pypackages__/

# Celery
celerybeat-schedule
celerybeat.pid

# SageMath parsed files
*.sage.py

# Environments
.env
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# Pyre type checker
.pyre/

# pytype static type analyzer
.pytype/

# Cython debug symbols
cython_debug/

# Application specific
config.json
email_config.json
feedback_config.json
*.db
*.sqlite
*.sqlite3
temp/
tmp/
cache/

# Freebuff desktop app data
.freebuff/

# Agent / test workspaces (sandbox artifacts, not source)
agent_workspace/
test_workspace/

# UI preview artifacts (local-only)
ui_preview.html
ui_preview_*.png
_bubble_*.png

# NOTE: float.png (floating chat button icon) and screen.png (README screenshot)
# are intentionally tracked - they are runtime assets referenced by the app.

# React frontend build output
frontend/dist/
frontend/node_modules/
frontend/.vite/

# Electron
electron/dist/
electron/node_modules/
release/

# Model files (usually large)
*.gguf
*.bin
models/*.gguf
models/*.bin

# User data created at runtime (chat sessions, exports, local config)
chats/
exports/
config/

# Local llama.cpp reference checkout + prebuilt binaries
llama.cpp/
llama.cpp-bin/

# Benchmarks
.benchmarks/

# Kiro folder
.kiro/

# Website repo (separate git repository, deployed via GitHub Pages)

# Website repo (separate git repository, deployed via GitHub Pages)

# Website repo (separate git repository, deployed via GitHub Pages)

# Website repo (separate git repository, deployed via GitHub Pages)

# Website repo (separate git repository, deployed via GitHub Pages)

# Website repo (separate git repository, deployed via GitHub Pages)

# Website repo (separate git repository, deployed via GitHub Pages)

# Website repo (separate git repository, deployed via GitHub Pages)

# Website repo (separate git repository, deployed via GitHub Pages)

# Website repo (separate git repository, deployed via GitHub Pages)

# Website repo (separate git repository, deployed via GitHub Pages)
ggufloader.github.io/

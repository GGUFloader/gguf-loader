# GGUF Loader Requirements
# Core dependencies for the GGUF Loader application

# NOTE: no PySide6 - the desktop UI is the React app running in Electron,
# so the Python side is backend-only (FastAPI + llama.cpp + LangGraph).

# API Server
fastapi>=0.115,<1
uvicorn>=0.30,<1

# AI/ML Dependencies
llama-cpp-python==0.3.34

# Agent framework (LangGraph over llama.cpp)
langgraph>=0.6.11,<0.7
langgraph-checkpoint-sqlite>=2.0.11,<3  # SqliteSaver for agent checkpointing (2.x matches langgraph 0.6)
langchain-core>=0.3.86,<0.4

# Optional GPU acceleration (uncomment if needed)
# llama-cpp-python[cuda]>=0.3.13  # For CUDA support
# llama-cpp-python[metal]>=0.3.13  # For Metal support (macOS)

# Development dependencies (optional)
# pytest>=8.0.0
# pytest-qt>=4.4.0